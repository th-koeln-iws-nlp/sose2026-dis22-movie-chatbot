Ihr könnt beim Retrieval mit allen möglichen Techniken arbeiten, die ihr kennen gelernt habt (dense, hybrid, ColBERT etc)

1. Implementiert einen RAG Workflow mit eurer Vektordatenbank und beiden LLMs in den Chatbot. Denkt daran, eure System Message anzupassen. RAG kann über einen Schalter/Button auf der Oberfläche an und aus geschaltet werden (default: an).
2. Fügt den Similarity Threshold und die Anzahl der abgerufenen Dokumente als einstellbaren Parameter hinzu.
3. Zeigt bei der generierten Antwort an, welche Dokumente aus der Vektordatenbank verwendet wurden.

